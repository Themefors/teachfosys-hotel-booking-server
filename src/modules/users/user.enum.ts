/* eslint-disable no-unused-vars */
export enum EGender {
  MALE = 'male',
  FEMALE = 'female',
  OTHERS = 'others',
}

export enum EStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
  DELETED = 'deleted',
}

export enum ERole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  STUFF = 'stuff',
  ACCOUNTS = 'accounts',
  USER = 'user',
}
